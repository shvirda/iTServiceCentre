# 🚀 Быстрый старт PromoService V0002

## За 1 минуту до запуска

### Шаг 1: Установка зависимостей (если не установлены)

```powershell
python run_safe.py
```

Этот скрипт автоматически:
- Проверит все зависимости
- Установит недостающие модули
- Запустит приложение

### Шаг 2: Запуск приложения

Если зависимости уже установлены:

```powershell
python run.py
```

Или безопасный запуск с проверкой:

```powershell
python run_safe.py
```

---

## 📱 Первый запуск

1. Откроется окно авторизации
2. Введите учетные данные:
   - **Username:** admin
   - **Password:** admin123
3. ⚠️ **Обязательно измените пароль!**

---

## 🔧 Если возникла ошибка

### Ошибка: "DLL load failed while importing QtCore"

```powershell
pip install --no-cache-dir --force-reinstall PyQt6==6.6.0
python run_safe.py
```

### Ошибка: "No module named..."

```powershell
pip install -r requirements.txt
python run_safe.py
```

### Полная переустановка зависимостей

```powershell
pip cache purge
pip install -r requirements.txt
python run_safe.py
```

---

## 💻 Варианты запуска

| Команда | Описание |
|---------|----------|
| `python run_safe.py` | **Рекомендуется** - с проверкой зависимостей |
| `python run.py` | Backend + Frontend одновременно |
| `python run_backend.py` | Только Backend (API на :5000) |
| `python run_frontend.py` | Только Frontend (нужен запущенный Backend) |

---

## 📚 Документация

- **INSTALL_GUIDE.md** - Подробное руководство установки
- **FIXES_SUMMARY.md** - Решение всех известных проблем
- **README.md** - Полная документация проекта

---

## ⌨️ Горячие клавиши

- `Ctrl+C` - Остановить приложение
- `Alt+F4` - Закрыть окно (Windows)

---

## 🆘 Нужна помощь?

1. Проверьте **INSTALL_GUIDE.md**
2. Посмотрите **FIXES_SUMMARY.md**
3. Прочитайте логи в папке **logs/**

---

**Готово!** Используйте `python run_safe.py` для запуска. 🎉

# PromoService V0002 - Управление сервисным центром

Полнофункциональное приложение для управления компьютерным сервисным центром с поддержкой локальной сети, БД, API с авторизацией и мультимодульным интерфейсом. Запуск backend и frontend одновременно из одного файла.

## Возможности

### 10 основных модулей:
1. **Управление БД** - Создание, подключение, миграции БД
2. **Ввод клиентов** - Добавление информации о клиентах сервиса
3. **Ввод техники** - Регистрация техники на ремонт
4. **Ввод товара** - Управление складом и товарами
5. **Сервисы** - Настройка Telegram, SMS, Email уведомлений
6. **Поиск клиентов** - Табличный поиск и фильтрация клиентов
7. **Поиск техники** - Табличный поиск техники и статус ремонта
8. **Поиск товара** - Управление складом и учет товаров
9. **Управление сотрудниками** - Добавление, удаление, уровни доступа
10. **Логирование** - История всех операций в системе

## Архитектура

- **Frontend**: PyQt6 (десктопное приложение)
- **Backend**: Flask (REST API)
- **БД**: SQLite (локально) / PostgreSQL (опционально)
- **Аутентификация**: JWT токены + bcrypt хеширование паролей
- **Сеть**: REST API на локальной сети

## Требования

- Python 3.8+
- pip

## Установка

### 1. Клонировать/загрузить проект
```bash
cd K:\PROJECTs-FACTORY\serviseCentr\promoserviceV0002
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Единый запуск Backend и Frontend
```bash
python run.py
```

Этот скрипт одновременно запускает:
- Backend API сервер (`http://127.0.0.1:5000`)
- Frontend приложение (PyQt6 окно авторизации)

На первый запуск будет создана администратор:
- **Username**: admin
- **Password**: admin123
- **Роль**: director

⚠️ **Обязательно измените пароль после первого входа!**

## Использование

### Основной способ запуска (рекомендуется)
```bash
python run.py
```

Запускает Backend API сервер и Frontend приложение одновременно.

### Запуск только Backend (API сервер)
```bash
python run_backend.py
```

API будет доступен на `http://127.0.0.1:5000`

### Запуск только Frontend (UI приложение)
```bash
python run_frontend.py
```

## Структура проекта

```
promoserviceV0002/
├── run.py                # ✨ Единый скрипт запуска (backend + frontend)
├── run_backend.py        # Запуск только backend
├── run_frontend.py       # Запуск только frontend
├── config.py             # Конфигурация
├── requirements.txt      # Зависимости
├── FILES_LOG.md          # Логирование структуры проекта
├── frontend/             # PyQt6 интерфейс
│   ├── modules/         # 10 модулей приложения
│   ├── ui/              # UI компоненты и стили
│   └── utils/           # Утилиты (API клиент, валидация)
├── backend/             # Flask API сервер
│   ├── api/             # API endpoints
│   ├── auth.py          # Авторизация и JWT токены
│   └── app.py           # Главное приложение
├── database/            # Модели БД
│   ├── models.py        # SQLAlchemy модели
│   └── db_manager.py    # Менеджер БД
├── tests/               # Тесты
├── logs/                # Логи приложения
└── instance/            # БД и временные файлы
```

## Уровни доступа

- **Director** (Директор) - Полный доступ ко всем функциям и логирование
- **Manager** (Менеджер) - Управление клиентами, техникой, товаром
- **Employee** (Сотрудник) - Просмотр и добавление данных
- **Warehouse** (Кладовщик) - Управление складом

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход пользователя
- `POST /api/auth/register` - Регистрация пользователя
- `GET /api/health` - Проверка здоровья API

### Модули (в разработке)
- `/api/clients` - Управление клиентами
- `/api/equipment` - Управление техникой
- `/api/warehouse` - Управление складом
- `/api/employees` - Управление сотрудниками
- `/api/services` - Управление сервисами
- `/api/logs` - Логирование операций

## Безопасность

- ✓ Хеширование паролей (bcrypt)
- ✓ JWT токены для API запросов
- ✓ Проверка ролей доступа
- ✓ Логирование всех операций
- ✓ CORS защита

## Первый этап разработки (MVP)

Реализовано:
- ✓ Структура проекта
- ✓ Модели БД (7 таблиц)
- ✓ Менеджер БД
- ✓ Backend с авторизацией
- ✓ API endpoints (заглушки)

В разработке:
- Frontend (PyQt6)
- Модули ввода данных (2, 3, 4, 9)
- Модули поиска (6, 7, 8, 10)
- API endpoints для всех модулей
- Интеграция сервисов (Telegram, SMS)

## Лицензия

Все права защищены

## Поддержка

Для вопросов и ошибок, пожалуйста, создавайте issues в проекте.

---

**Версия**: 0.0.2 (Alpha)
**Статус**: В разработке
**Последнее обновление**: 2025-12-29

## Изменения в версии V0002

- ✨ Создан единый скрипт запуска `run.py` для одновременного запуска backend и frontend
- 📝 Добавлено логирование структуры проекта в файл `FILES_LOG.md`
- 🔧 Улучшена конфигурация и структура проекта
"# iTServiceCentre"  
